# Freddy Fix It — CLAUDE.md

Calgary home-services marketplace. Clients post a request → admin/contractors quote or bid → job lifecycle → review.
Live: https://freddyfixit.ca (+ www), https://freddy-fixit.vercel.app

The owner is **non-technical** and deploys by pasting terminal commands. Optimize every answer for that:
ship one runnable thing, keep explanations short, never assume CLI fluency.

## Stack
- **Frontend:** React 19 + TypeScript + Vite + Wouter (routing) + Framer Motion. Build = `vite build` (esbuild, **no typecheck** — type errors don't block deploys).
- **Backend:** Supabase only (Postgres + Auth + RLS + Storage + Edge Functions). No app server. "API" = PostgREST (`supabase.from(...)`) for reads + `SECURITY DEFINER` RPCs / edge functions for privileged writes.
- **Deploy:** Vercel auto-deploys from GitHub `main`. Email via Resend. DNS via Cloudflare.
- Details by area: see `src/CLAUDE.md` (frontend) and `supabase/CLAUDE.md` (DB/functions).

## How work ships (READ THIS FIRST)
Repo `github.com/karanmann137-creator/freddy-fixit` is **PUBLIC**. The assistant can clone but **cannot push**; the owner deploys by running installer scripts.

1. Clone once per session to `/home/claude/ff4`. This clone is **cumulative** — keep editing it across turns; don't re-clone fresh mid-project (you'd lose un-pushed edits).
2. Edit files in the clone (`str_replace`/`create_file`). Validate with `npx vite build` before shipping.
3. Emit ONE installer `.sh` to `/mnt/user-data/outputs/` that base64-decodes each changed file into `~/freddy-fixit/`, then `git add -A && commit && push`. Verify the installer round-trips (decode == source) before presenting.
4. Owner runs: `bash ~/Desktop/Website/apply-<x>.sh`, waits for `✅ … pushed`, then hard-refreshes (Cmd+Shift+R).

**Installer rules:**
- Installers are **cumulative per file** and **supersede** older installers that touch the same files. Owner runs **only the newest**; running an older one after a newer one reverts changes.
- When in doubt, ship a **superset** installer (include every file from prior installers + the new ones) so "run the newest" always delivers everything.
- **DB changes are NOT in installers** — apply them live via Supabase MCP tools (migrations / RPCs / edge-function deploys). Edge-function source is also committed to the repo (via installer) for version control, but deploying it is a separate Supabase step.

## Build / validate commands
```
cd /home/claude/ff4
npm install            # first time
npx vite build         # full build (catches import/JSX errors; no typecheck)
npx esbuild <file> --loader:.tsx=tsx   # quick single-file parse check
```

## Brand tokens
Navy `#1a2236` · section navy `#151d2e` · footer `#111827` · orange `#ea6b14` · text `#f0f4ff`.
Fonts: Bebas Neue (headings), DM Sans (body). Loaded per-page via a Google Fonts `<link>`.

## Current feature state (live unless noted)
- Auth + role-gated dashboards (client / contractor / admin). Auth-deadlock fix in place (never call supabase queries inside `onAuthStateChange`).
- Job lifecycle: assign → contractor proposes time+price → client approves → contractor completes+photo → client confirms → auto-confirm after 3 days (pg_cron). In-app 🔔 + email notifications.
- Bidding (max 3 bids/request), client-picks-from-bids, admin assign/approve.
- Contractor onboarding with vetting (licensed/insurance/WCB/references) + company name; admin review line; FAQ on home; full service-category list.
- **Self-serve account deletion** (`delete-account` edge fn + `DeleteAccount` component) with re-signup flagging for poorly-rated contractors. See `supabase/CLAUDE.md`.
- **Claims / dispute process.** Client files a formal claim (`ReportProblem` → `open_dispute` RPC: structured fields — reason, service date, agreed scope, requested remedy, amount, signed declaration + photos) which freezes payment to `disputed` and notifies contractor + all admins (in-app + email via notifications→send-notification). Contractor sees the claim on their job and responds within a 3-day window (`RespondToClaim` → `respond_to_dispute` RPC). Admin reviews both sides in the disputes tab and resolves via `resolve-dispute` edge fn (full/partial refund or release). Photos in private `problem-photos` bucket; dispute parties can read each other's via RLS.
- **Returning-client new-request flow** (`NewRequest`): signed-in users skip signup and get a short form ("same address as last time?"). `ClientOnboarding` branches logged-out→signup, logged-in→NewRequest.
- Code-review pass done: client-dashboard query parallelized, server-side Browse specialty filter (GIN), admin pagination, admin status RPC, RLS `(select auth.uid())` wrapping, contractor-earnings single source of truth (jobs → trigger).

- **SEO service landing pages (2026-06-27).** `/services` index + `/services/:slug` (ServicesIndex.tsx / ServiceLanding.tsx) target Calgary trade searches ("handyman calgary", "calgary plumber", etc.). 18 slugs, each with per-page meta + Service/FAQPage JSON-LD, "what we cover"/"how it works"/FAQ/related-services sections, CTAs to /client-onboarding?service=<name> (name matches Home SERVICES label). Site-wide LocalBusiness+WebSite JSON-LD in index.html. Sitemap + footer carry internal links to all service pages.

- **Homepage conversion rework + contractor recruitment (2026-06-27).** Hero tagline now "Calgary's Vetted Handymen & Trades — On Demand"; client "I Need a Fix" card is primary (first + orange-bordered) with CTA "Get my free quote". New `/for-contractors` recruitment landing page (ForContractors.tsx) — value props (no fees/lead-buying, secure payout, local jobs), how-it-works, FAQ+JSON-LD, CTA→/contractor-onboarding; linked in footer + sitemap. Offline deliverables (owner's Desktop, not in repo): Google-Business-Profile-Guide.docx, Contractor-Recruitment-Kit.docx.

- **Social proof + analytics + SEO batch (2026-06-27).** (1) Homepage "Built On Trust" section now surfaces real client reviews dynamically via new `get_homepage_reviews(p_limit)` SECURITY DEFINER RPC (reviews with a comment, reviewer first name + contractor company name; granted to anon/authenticated). Graceful empty state = the three honest trust cards (no fabricated testimonials; only 1 review with no comment exists in DB). (2) Google Analytics 4 scaffolding: `src/lib/analytics.ts` (placeholder `GA_MEASUREMENT_ID = "G-XXXXXXXXXX"` — OFF until a real ID is pasted; IP anonymized, send_page_view:false). `initAnalytics()` in main.tsx, `trackPageView()` on route change in App.tsx ScrollToTop, conversion events `generate_lead` (GetQuote), `post_job`+`sign_up` (ClientOnboarding), `sign_up` (ContractorOnboarding). Privacy Policy §11 updated to disclose GA4 + cookies + opt-out, and Google LLC added to service-provider table. Owner deliverable: Analytics-Setup-Guide.docx (GA4 + Search Console walkthrough, on Desktop). (3) 3 new Calgary SEO blog posts: calgary-electrician-cost-2026, calgary-furnace-repair-replacement-cost-2026, hiring-a-contractor-calgary-questions-permits (Blog.tsx + BlogPost.tsx article components + POST_DESCRIPTIONS + sitemap). 
- **Pending owner input:** seed contractors Slone (a01c49f7-0ba6-4e0a-bc81-aba53baebcb7) and Justin (44748517-72d6-440a-ab06-b13e2660cc80) still have NULL company_name + vetting answers — apply real values via Supabase MCP once owner provides them.

- **Google review popup (2026-06-27).** `src/lib/reviewPrompt.ts` fires a `window` CustomEvent `ff:google-review`; `src/components/GoogleReviewModal.tsx` (brand navy/orange card, 5 stars) listens for it and is mounted once in App.tsx beside ChatWidget. Triggered at three moments only: account created + job posted (ClientOnboarding both success/verify branches; NewRequest after returning-client post) and job done (ClientDashboard confirmCompletion after RPC). localStorage dedupe: signup/post each ask once ever, job_done dedupes per jobId, ~21-day cooldown between any prompts, "Don't ask again" opt-out. **OFF until owner pastes the platform Google Business Profile review URL** into `GOOGLE_REVIEW_URL` (placeholder contains REPLACE_WITH → popup never shows while placeholder).

- **Base prices + quote ranges (2026-07-02).** New `service_pricing` table (service label PK → base_price / typical_low / typical_high / unit) seeded with Calgary-market figures for all 22 services; public read via `get_service_pricing()` SECURITY DEFINER RPC (anon+authenticated). Shared client lib `src/lib/servicePricing.ts` (`useServicePricing()` hook, cached once/session; `rangeText`/`fromText`/`money` helpers). **Clients** see "from $X" under each service in the ClientOnboarding + NewRequest pickers and a "Typical range: $low–$high" hint on the GetQuote lead form. **Contractors** get a base-price reference box on every quote form (propose / bid / re-quote) with a one-tap **"Use base price"** button (fills amount, sets `used_base_price`), plus optional **min–max range inputs** (`price_low`/`price_high`, prefilled with the platform typical range as placeholders). Available-jobs list shows the service's typical range. `bids` + `jobs` gained `price_low`/`price_high`/`used_base_price` columns; `place_bid`, `propose_job_schedule`, `request_quote_revision` all extended with `p_price_low`/`p_price_high`/`p_used_base_price` (old signatures DROPped first to avoid the overload-ambiguity trap). Client sees the approved range in `QuoteBreakdownView` ("Estimated range: $X–$Y").

- **Job-matching upgrade (2026-07-02).** The "Available Jobs" feed (`list_open_jobs()`) was a global open board — every active contractor saw every pending request regardless of trade. Rewrote it to a real matcher: (1) **hard specialty filter** via new `service_specialty_map` table (client `service_needed` label -> required contractor `specialties`; bridges the two label vocabularies, e.g. "General Handyman"->{General Repairs}, "Drywall / Flooring"->{Drywall,Flooring / Tile}). Unclassifiable services (`Other` / any label with no map row) pass through to everyone so jobs are never invisible. (2) **Preferred-pro reservation**: a request with `preferred_contractor_id` (rehire flow) is visible/biddable ONLY to that pro for 48h, then opens to matched contractors; enforced in both `list_open_jobs` and `place_bid` (raises if a non-preferred pro bids inside the window). (3) **Ranking**: my-reserved-jobs -> fewest bids -> newest. (4) **Query cleanup**: replaced the 3 per-row correlated subqueries + double location-regex with LATERAL joins (single regex). (5) New composite index `client_requests(status, created_at desc)` (dropped redundant `client_requests_status_idx`). `list_open_jobs` gained an `is_preferred` bool -> ContractorDashboard shows a "This client requested you — reserved for 48h" badge. All DB changes applied live via MCP; only ContractorDashboard.tsx (badge + copy) ships in the installer. Note: `assign_job`/`accept_bid` (admin/client picks) are unchanged.
- **Retention + organic-growth batch (2026-07-02).** Six features, all wired email + in-app (existing Resend + notification bell; NO SMS).
  1. *Contractor alerts* (new lead / won bid / payout released) were already built — verified, no change.
  2. *Time-based earnings + weekly goal* (ContractorDashboard earnings tab): `get_contractor_earnings_stats()` RPC (this week/month/last month/year/lifetime + pending + 8-week `trend`), `set_weekly_goal(p_goal)`, `contractors.weekly_goal` column. UI: This-Week hero, goal progress bar, 8-week bar chart.
  3. *Rehire your pro + favorites* (ClientDashboard "Your pros"): `favorites` table + `toggle_favorite(uuid)`, `list_my_pros()` (worked-with OR favorited, jobs_together/last_service/rating), `client_requests.preferred_contractor_id` + `notify_preferred_contractor()` trigger; NewRequest reads `?pro=` and pre-targets the pro.
  4. *Freddy Rewind share cards* (`src/components/FreddyRewind.tsx`, mode client|contractor): `get_contractor_rewind()` / `get_client_rewind()` RPCs; canvas 1080x1350 PNG download + navigator.share/clipboard, embeds referral link `freddyfixit.ca/?ref=<code>`. "My Rewind" button on both dashboards.
  5. *Seasonal + recurring reminders* (pg_cron `daily-reminders` 16:30 UTC → `run_reminders()`): recurring-due nudges from completed jobs + one Calgary-specific seasonal nudge per engaged client per season; `notify_user()` helper (in-app + `send-reminder` edge fn email), `reminder_log` dedupe.
  6. *Referral system — first-job 3% fee waiver.* `profiles.referral_code`/`referred_by` (auto-assigned, `gen_referral_code()`), `referrals` table, `apply_referral_code(p_code)` / `get_my_referral()` / `consume_referral_waiver()` / `referral_waiver_eligible()` RPCs. App.tsx captures `?ref=` → localStorage; ClientOnboarding (session branch) + AuthCallback (email-confirm branch) call `apply_referral_code`. **create-payment-intent** (v13) waives the 3% for an eligible referred client's first job (read-only check); **stripe-webhook** (v10) consumes the reward only on `payment_intent.succeeded`. ClientDashboard "Invite a friend" card shows code + copy. New edge fn `send-reminder` (verify_jwt=false). All new RPCs coalesce to zero/empty on the currently-empty marketplace.

- **Matcher unification + location ranking (2026-07-02).** All three matchers now share the single `service_specialty_map` source of truth (previously they disagreed). (1) `notify_contractors_new_request()` (in-app 🔔 on new request) dropped its fuzzy `service_needed ILIKE '%'||specialty||'%'` test — which failed to alert handymen about handyman jobs — for the same map join (`specialties && required`, unmapped/`Other` -> notify all). It also now short-circuits when a request is reserved for a preferred pro (the dedicated `notify_preferred_contractor()` handles that). (2) `dispatch-job` edge fn (v10, verify_jwt=false, now committed to repo at `supabase/functions/dispatch-job/index.ts`) switched its candidate filter from the keyword `tradeMatch` heuristic to the map, and now respects the 48h `preferred_contractor_id` reservation (emails only the requested pro inside the window) and skips `hidden_jobs`; the keyword `score()` is kept ONLY to order the emails. (3) `list_open_jobs()` feed gained a **location ranking tier** (rank-only, never a filter): jobs whose Calgary zone (NW/NE/SW/SE from `location`) is in the contractor's `service_area` float to the top; order is now reserved -> in-zone -> fewest bids -> newest. Out-of-zone and town jobs (Airdrie/etc, no zone token) stay visible, just lower. All DB/edge changes applied live via MCP; the installer ships the dispatch-job source for version control only.

- **Audit fixes: payment safety net + dashboard hardening (2026-07-02).** Follow-up to the matcher audit, fixing every flagged item.
  1. *Widened location ranking* in `list_open_jobs()` — the in-zone tier now recognizes Downtown/Beltline and towns (Airdrie/Cochrane/Chestermere/Okotoks/Strathmore), not just NW/NE/SW/SE; display `location` labels these cleanly.
  2. *Security-hardened matchers* — `accept_bid` gained `SET search_path`; `place_bid` gained a `pg_advisory_xact_lock(hashtext(request_id))` to close the 3-bid-cap check-then-insert race.
  3. *Payment-release safety net.* `release-payment` (v11) now accepts an internal service-role bearer so a server job can drive it. New `reconcile-payouts` edge fn (verify_jwt=false) finds any `completed` + `held` + client-confirmed + un-disputed job and releases it via release-payment (idempotent). Wired to pg_cron `reconcile-payouts` every 15 min via `public.kick_reconcile_payouts()` (net.http_post, anon bearer). This fixes BOTH the swallowed client-side release error AND the long-standing bug that auto-confirmed jobs (`auto_confirm_stale_jobs`) never released funds. ClientDashboard `confirmCompletion` now surfaces a reassuring "payment processing, nothing to do" message instead of `.catch(()=>{})`.
  4. *ClientDashboard* — profile read `.single()`->`.maybeSingle()` in a try/catch/finally so loading never sticks; a load failure shows an error+Retry screen instead of a false "no jobs" empty state; the 3% fee display is waived per-job when `referral_waiver_eligible(client, job)` is true (matches what Stripe charges); aria-label on the favourite heart.
  5. *ContractorDashboard* — `openJob` now restores `price_low`/`price_high`/`used_base_price` (re-opening a job no longer wipes a saved range); profile/contractor reads use `.maybeSingle()`; availability save checks the update `.error` and rolls back the optimistic write, and rejects an end-time at/before the start.
  6. *AdminDashboard* — per-request assign busy state (was one global flag freezing every row) that clears the selection + ranking on success and patches the row instead of a full `loadAll()`; the assign dropdown is now **best-fit ranked** via new `admin_rank_contractors(request_id)` SECURITY DEFINER RPC (same `service_specialty_map` + zone logic, groups "Best matches" with a ★ for in-zone vs "Other contractors"); dispute claim + response photos are signed in one parallel pass (was two sequential per-dispute loops); Approve/Deactivate got a per-contractor busy guard + error feedback + local state patch. All DB/edge changes live via MCP; the installer ships release-payment + reconcile-payouts source for version control.

- **Guardrails: typecheck CI + single-source service fee + health-check (2026-07-03).** Hardening so two drift-prone things can't silently break.
  1. *Typecheck gate.* esbuild/vite still ship with no typecheck, but there's now a committed `tsconfig.check.json` (extends `tsconfig.app.json`, adds the `@/`->`./src` path alias tsc was missing, turns off unused-var lint noise) + `npm run typecheck` + a non-blocking GitHub Action (`.github/workflows/typecheck.yml`, runs on push/PR to main). Baseline is **0 errors** — so any NEW type error (e.g. a field renamed on one side only) shows a red ✗ on the commit before it reaches users. Reaching 0 also fixed two real latent bugs: `Ic` had no `star`/`check` icons so `<Ic name="star"/>` (rating stars, "My Rewind" buttons, reserved badge) and `name="check"` rendered **blank** — both added to the icon set; and `UserRole` was imported by App.tsx but never exported from `lib/supabase.ts` — now exported. Plus small Supabase-RPC-union casts in servicePricing/AdminDashboard/ClientDashboard.
  2. *Single source of truth for the 3% service fee.* The bare `0.03` used to live in BOTH `create-payment-intent` and ClientDashboard, so display and charge could drift. New `platform_fee_rate()` DB fn is the ONE place the rate lives; `create-payment-intent` (v14) reads it (fallback 0.03, never blocks checkout) and ClientDashboard loads it into `feeRate` (so the shown total is exactly what Stripe charges). Also new `get_job_fee(client, job)` returns the canonical {base, rate, fee, total, waived} breakdown. Receipt line now shows the actual % applied (0% when a referral waives it).
  3. *Daily money-path health-check.* `platform_health_check()` returns 4 named checks (fee rate sane; every charged `client_fee` == 0 or amount*rate; critical RPCs present; no confirmed+held payout stuck >2h). `run_platform_health_check()` (pg_cron `platform-health-check`, 15:00 UTC daily) alerts every admin in-app via `_notify` ONLY when a check fails (deduped ~20h); silent when healthy. All DB/edge changes live via MCP; the installer ships create-payment-intent source + the new frontend/config files for version control.

- **Milestone escrow for big jobs (2026-07-03).** Jobs quoted **> $2,000** can now be paid in **2–5 escrow-protected, fund-as-you-go stages** instead of one upfront charge; small jobs (≤ $2k) keep the existing single-charge flow untouched (`is_milestone` gates everything). Economics are identical to charging the whole job once — proportional **3% client fee + 7% commission (93% payout)** per stage. Journey: contractor builds a milestone plan on the quote (auto-suggest Deposit 20 / Rough-in 30 / Substantial 30 / Final 20, amounts must sum to the quote) → client approves the schedule (nothing charged) → per stage, client **funds** it (Stripe Checkout, funds HELD), contractor **completes + photo**, client **approves & releases** (93% → contractor) or **disputes** (freezes just that stage); **3-day per-stage auto-approve** safety net; stages fund strictly in order; next stage unlocks after the prior releases. **DB (live via MCP, not in installer):** `job_milestones` table (`pending→funded→completed→released`, +`disputed`; per-stage fee columns, Stripe refs, photo, timestamps; RLS admin/client/contractor of the job) + jobs.`is_milestone`/`milestone_schedule_status` + 7 SECURITY DEFINER RPCs (`get_job_milestones`, `propose_milestones`, `approve_milestone_schedule`, `complete_milestone`, `approve_milestone`, `dispute_milestone`, `auto_approve_stale_milestones`). **Edge (live via MCP; source committed here for version control):** new `create-milestone-payment` (v1, verify_jwt=true — charges one stage + fee, order/approval/dispute guards, referral waiver on seq 1 only); `release-payment` (v12 — new milestone mode `{milestone_id}`, idempotencyKey `payout_<milestone_id>`, releases 93%, marks job complete when all stages released; job mode unchanged); `stripe-webhook` (v11 — flips stage pending→funded on `payment_intent.succeeded` for `metadata.kind==='milestone'`, consumes referral waiver on seq 1; single-charge path unchanged); `reconcile-payouts` (v2 — first `auto_approve_stale_milestones(3)`, then releases stuck single-charge jobs AND completed+approved+un-disputed stages; still covers the old auto-confirm gap). **Frontend (ships in installer):** self-contained `src/components/MilestonePanel.tsx` (role `contractor|client|admin`) mounted in ClientDashboard (client, when `is_milestone`), ContractorDashboard (contractor, when `amount>2000`), AdminDashboard (admin, read-only + manual release). The whole-job "pay"/mark-complete branches are gated with `!is_milestone` so milestone jobs use the panel instead; the completed-job review/rating flow is untouched. Referral 3% waiver applies to the **first funded stage only**. Reuses `platform_fee_rate()` (fallback 0.03, never blocks checkout). **Legal (2026-07-03 follow-up):** User Agreement gained **§6.8 Milestone (Staged) Payments** + **§6.9 Concerns, Cancellations and Refunds** (client-side: approve-before-charge, fund-in-order, never obligated to fund later stages, per-stage 3-day release, per-stage refund limited to still-held funds, non-waiver of Alberta consumer-protection rights) and **Contractor Terms §9.4 Staged (Milestone) Jobs** (per-stage freeze/refund). Consumer-facing **"escrow" wording softened to "held securely"** (SettingsModal, ContractorDashboard) to avoid misdescribing the Stripe held-balance as a trust/escrow account; UI/panel copy already avoided the word. Open item routed to counsel: Alberta prepayment/deposit + cooling-off rules on staged funding for jobs >$2k. **Alberta prepaid-contracting research + terms (2026-07-03).** Researched Alberta Consumer Protection Act + Prepaid Contracting Business Licensing Regulation + Direct Sales Cancellation regs. Key finding: money collected **before work is complete** for home-services performed away from a fixed storefront can make a job a **"prepaid contract"** under Alberta law — this applies to BOTH the single-charge flow AND the milestone flow, not just >$2k jobs. Added two clauses to `UserAgreement.tsx`: client **§6.10 "Your Cancellation Rights Under Alberta Law"** (discloses pre-completion payment; 10-day no-penalty cancel after receiving contract copy; 30-day non-start → 1-year cancel; refund of unreleased deposits within 15 days of valid cancellation; how to cancel; non-waiver — law prevails over conflicting terms) and contractor **§1.e "Prepaid Contracting Licence (Alberta)"** (puts the burden on the contractor to determine if they need a **Service Alberta prepaid-contracting licence + security bond**, maintain it, use a compliant written contract, and honour cancellation refunds; Company may require proof + suspend). **⚠️ Counsel/ops flag (terms text alone does NOT satisfy this):** prepaid-contracting is a **licensing** obligation. Contractors may each need a licence + ~$10k bond; and the **platform itself** may qualify as a prepaid contracting business requiring its own licence — a lawyer + Service Alberta must confirm. Not resolvable by wording. **Prepaid-contract compliance mechanics shipped (2026-07-03).** Owner decision: KEEP collecting pre-completion payment (both single-charge and milestone) and add compliance safeguards rather than pause. (1) **Written contract copy emailed at quote approval** — new `contract_copy` event in `notify-email` (v6) builds a full written agreement (parties, service, address from `client_requests.location`, scope from `job_description`, start date, single-charge total OR staged per-stage payment table, and an Alberta cancellation-rights box) and stamps new `jobs.contract_copy_sent_at` (the audit timestamp the 10-day clock runs from). Fired fire-and-forget after the client approves: `approve_job_schedule` (ClientDashboard) and `approve_milestone_schedule` (MilestonePanel). (2) **Milestone 15-day refund path built** — audit found single-charge refunds worked (resolve-dispute refunds the held PaymentIntent) but milestone stages had freeze-only, NO refund action. Added: `job_milestones.status` now allows `refunded` (+`refunded_at`,`stripe_refund_id` cols); new **`refund-milestone`** edge fn (v1, verify_jwt=true, admin-only) refunds a still-held stage (funded/completed/disputed) via `stripe.refunds.create` on the stage PaymentIntent (idempotencyKey `mrefund_<id>`), marks it `refunded`; rejects already-released/refunded/unfunded. MilestonePanel admin mode got a **"Refund stage to client"** button + `refunded` status chip. (3) **Terms** — UserAgreement §6.10 updated to note the written copy is emailed at approval and the 10-day clock runs from receipt. **DB/edge live via MCP; installer ships notify-email + refund-milestone source + the frontend/terms changes.** **⚠️ Still open (unchanged):** the prepaid-contracting LICENSING question (contractor licence + bond, and whether the platform itself needs a licence) is NOT resolved by any of this — still needs a lawyer + Service Alberta.

- **Recurring booking overhaul + pay-ahead escrow (2026-07-04).** Reworked how recurring jobs are booked and paid. (1) **Category-aware cadence** — new shared `src/lib/recurrence.ts` (`FREQ_LABELS`, `recurrenceOptionsFor(services)`, `isPerKmService`, `cadenceHint`, `type Freq`) maps each service to a sensible menu instead of a fixed weekly/biweekly/monthly/seasonal list. Cadences now cover weekly, biweekly, monthly, quarterly, seasonal, semiannual, annual and **per_km** (vehicle services: Oil Change / Tire Swap / Battery-Brakes / Vehicle Maintenance). Per-km can't read an odometer, so it becomes a conservative time nudge (DB `recurrence_interval('per_km')` = interval '4 months'). ClientOnboarding + NewRequest render the category menu, a per-km input, and a **"Pay ahead?"** preference (Pay each visit / Prepay 2 / Prepay 3 → captured on `client_requests.recurring_prepay_pref`; `recurring_interval_km` also captured). `handle_new_user` trigger extended to persist both new metadata fields. (2) **Auto-generation** — `generate_recurring_occurrences()` (SECURITY DEFINER) creates the next occurrence request when one is due (anchored on last completed visit + `recurrence_interval(frequency)`), copying the plan, reserving the same pro via `preferred_contractor_id` (reuses the 48h reservation), and notifying client + pro. Hooked into the existing `run_reminders()` daily cron (step 0). Plan columns on `client_requests`: `recurring_plan_status` (active|paused|ended), `recurring_next_due`, `recurring_parent_id`. **No auto-charge** — the client still approves/pays each occurrence. (3) **Prepay escrow (pool)** — one Stripe charge covers N visits held in a pool, released 93% to the pro per completed+confirmed visit (3% client fee + 7% commission per visit, identical economics to charging each once). New `recurring_prepayments` table (pending→held→partially_released→released; refunded/canceled) + `jobs.prepayment_id`/`prepayment_seq`. RPCs: `list_my_recurring_plans`, `set_recurring_plan_status`, `get_recurring_prepay_quote`, `consume_prepaid_occurrence` (draws down one held occurrence when a prepaid job is approved — no separate checkout). Edge: **create-recurring-prepayment** (v1, jwt) Stripe Checkout for the pool (`metadata.kind='recurring_prepay'`); **stripe-webhook** (v12) flips pool pending→held on success; **release-payment** (v14) new prepayment mode `{prepayment_id, job_id}` transfers 93% + increments `occurrences_released`; **reconcile-payouts** (v3) branch 1b releases confirmed prepaid jobs (and branch 1 now excludes `prepayment_id` jobs); **refund-recurring-prepayment** (v1, admin) refunds unreleased pool balance. UI: ClientDashboard "Recurring plans" card (prepay 2/3, pause/resume, remaining-visits), approval draws down the pool + confirm releases per visit; AdminDashboard new **Prepaid** tab with per-pool refund. Prepay is a **post-quote dashboard action** (no price exists until the first quote is approved); booking only captures the preference. Unused prepaid visits are refundable (aligns with the Alberta pre-completion-payment cancellation duties already documented). DB + edge live via MCP; installer (`apply-recurring-prepay.sh`, superset of the milestone installer) ships the frontend + edge source.

- **Recurring cadence slider + multiple specific dates (2026-07-04).** Replaced the recurring "How often?" chip row with a **slider** (6 stops: 1wk/2wk/3wk/1mo/2mo/3mo → tokens `weekly, biweekly, every_3_weeks, monthly, every_2_months, quarterly`), kept **Seasonal** as a separate toggle button (still shows the season-preset start/end shortcuts), and kept the **Per distance (km)** button for vehicle services (`isPerKmService`); a "Use slider instead" button reverts from Seasonal/per_km. Below the slider are clickable tick labels + a live cadence label. Added an optional **"Add specific visit dates"** multi-date picker (date input + "+ Add date" → removable chips) so a client can pin exact extra visits *on top of* the regular cadence. **DB (live via MCP):** `client_requests.recurring_dates date[]` + `recurring_source_date date`; `freq_interval()` rewritten to plpgsql that regex-parses `every_N_weeks`/`every_N_months` (falls back to the old CASE); `generate_recurring_occurrences()` gained a **specific-dates branch** — each date in `recurring_dates` materializes into a real queued `client_requests` occurrence when its 7-day lead window arrives (deduped via `recurring_source_date`, reserves the plan's pro, notifies client+pro), independent of the single-outstanding cadence guard; the cadence branch now only counts cadence occurrences (`recurring_source_date is null`) and only runs when a frequency is set. `handle_new_user()` parses `meta->'recurring_dates'` (JSON array) into `date[]`. **recurrence.ts:** new `SLIDER_STOPS`, `SLIDER_SHORT`, and a general `freqLabel(token)` that formats any token including `every_N_*`; `FREQ_LABELS` still exported. Frontend: ClientOnboarding (metadata insert) + NewRequest (direct insert) send `recurring_dates`; ContractorDashboard + ClientDashboard recurring labels now use `freqLabel` so new tokens render cleanly. Frontend ships in the installer (`apply-recurring-prepay.sh`, unchanged superset file list); DB is live via MCP.
- **Day-before visit confirm/change + recurring-commitment info bar (2026-07-04).** Two client-retention/reliability touches on the scheduled-job lifecycle. (1) **Contractor recurring info bar** — `list_open_jobs()` now returns `is_recurring`+`recurring_frequency` (DROP+recreate; added to the RETURNS row), and ContractorDashboard's my-jobs select pulls `recurring, recurring_frequency` from the `client_requests` join. An orange "Recurring job — <cadence>" banner shows on both the Available-Jobs card ("winning this makes you the client's go-to pro — you'll be expected to return at the agreed times") and the assigned/scheduled my-job card ("committing to show up at the agreed times each visit"), using `FREQ_LABELS` from `src/lib/recurrence.ts`. Icon: `refresh` (there is NO `repeat` glyph in `Ic`). (2) **Day-before confirm-or-change flow (universal on scheduled jobs, since every job already has a contractor-proposed time).** New `run_reminders()` **step 3**: for `status='scheduled'` jobs with `scheduled_at` within the next 28h, `visit_reminder_sent_at IS NULL` and not yet confirmed, sends an in-app 🔔 + email via `notify_user` ("confirm or change") and stamps `visit_reminder_sent_at`. New jobs columns: `visit_reminder_sent_at`, `client_confirmed_visit_at`, `client_rescheduled_at`, `reschedule_accepted_at`. New RPCs: `confirm_visit(job)` (client marks the time good → notifies contractor), `client_reschedule_visit(job, ts)` (client picks a new time → sets `scheduled_at`, flips job back to `status='assigned'` with `schedule_proposed_at=now()`, clears `client_approved_at`/confirm, stamps `client_rescheduled_at`, and warns+notifies the contractor via `notify_user`), `contractor_accept_reschedule(job)` (contractor accepts the client's new time → back to `scheduled`, `client_approved_at`+`reschedule_accepted_at` set, notifies client). If the contractor instead **declines**, they just re-use the existing **propose_job_schedule** form (now also clears the three reschedule markers so the client returns to the normal Approve flow). ClientDashboard: scheduled block gained a confirm/change card ("✓ Confirm this time" / "Change the time" → `datetime-local` + a warning that the pro must re-accept and may decline) and a "Waiting for your pro to accept the new time" state (the normal Approve-schedule block is suppressed while `client_rescheduled_at && !reschedule_accepted_at`). ContractorDashboard: assigned block gained a blue "The client changed the time" banner + "Accept new time" button (`contractor_accept_reschedule`); the existing propose form is right below it for decline+counter-propose. The soft `requestReschedule` (message-only "suggest a time") is untouched and still available on the pre-approval proposal. All DB live via MCP; installer (`apply-recurring-prepay.sh`, now a superset of this too) ships the two changed dashboards. Frontend-only for the installer; no new edge functions.
- **Signup-easier + media messaging + chatbot-on-home batch (2026-07-05).** (1) **Google one-tap moved to the TOP of step 1** in ClientOnboarding (was buried on the final account step) and confirmed already-top in ContractorOnboarding; client can create an account in one tap, or fall through to "or tell us what you need". (2) **Contractor "sign up now, finish later"** — a fast-track card appears on step 2 once specialties are picked (terms checkbox + "Create my account — finish later" → reuses `handleSubmit`, which already tolerates empty area/work-type/docs). The rest (service area, trade, licensing/insurance/WCB, references, verification docs) is completed later from the dashboard via new `src/components/ContractorProfileCompletion.tsx`, mounted at the top of the ContractorDashboard **profile tab** inside a "Finish setting up your profile" nudge card that lists exactly what's still missing (service area / trade / licence-or-insurance / documents) and hides once complete. Availability keeps its existing auto-save editor lower on the tab. (3) **Keyless address autocomplete** — new `src/components/AddressAutocomplete.tsx` uses Photon (photon.komoot.io, OpenStreetMap, **no API key**), Calgary-biased (lat/lon + Alberta-first sort), 280ms debounce, keyboard nav, click-outside close, and **silently degrades to plain typing** on any fetch error. Wired into the client location field in ClientOnboarding + NewRequest (new-address input). (4) **Trimmed signup fields** — phone is now **optional** up front in both flows (only validated if non-empty), all identity inputs got `autoComplete` attributes (given-name/family-name/email/tel/new-password) for browser autofill, and phone **auto-formats** as typed (403-555-0100 via `fmtPhone`). Both flows already show a segmented progress bar; added "Takes about 2 minutes" to the contractor intro line to match the client one. (5) **Chatbot (ChatWidget) hidden on dashboards, kept on home + everywhere else** — ChatWidget is now route-aware (`useLocation` from wouter) and returns `null` on `/client-dashboard`, `/contractor-dashboard`, `/admin-dashboard`. (6) **Image + video messaging in job chat, both sides, validated pre-upload.** New private `message-media` storage bucket (image/jpeg,png,webp,gif,heic,heif + video/mp4,webm,quicktime; 50MB cap) + `public.messages.attachment_path`/`attachment_type` columns + storage RLS (SELECT/INSERT for the job's client_id/contractor_id or admin; path convention `<job_id>/<file>`, first path segment must match the job). `JobChat.tsx` gained an attach button, hidden file input (accept images+video), **client-side guardrails before any upload** (type whitelist + images ≤10MB / videos ≤50MB, with a friendly error), a thumbnail preview with remove, optional caption, attachment-only messages, and renders images (click to open full) + `<video controls>` bubbles via 1-hour signed URLs (`createSignedUrls`, cached in state, signed on load + on realtime insert). DB/bucket/RLS live via MCP; frontend ships in the installer.

- **Full audit + fix batch (2026-07-05).** Three-track audit (UX/flow, security, legal) followed by fixes across five areas. (1) **contractor-outreach hardened (v12, live via MCP):** POST now requires an authenticated **admin** (anon-key getUser → profiles.role check); public unsubscribe (`?u=<uuid>`, GET **or** POST — Gmail one-click sends POST per RFC 8058) handled BEFORE the admin gate, UUID-validated, marks all rows with the same email `unsubscribed`, always returns the same page (no id-existence leak); email footer gained a mailing-address slot + one-click unsubscribe link + "reply unsubscribe"; Resend payload sends `List-Unsubscribe`/`List-Unsubscribe-Post` headers. Owner supplied the CASL physical address (20 Whiteram Mews NE, Calgary) — set in `MAILING_ADDRESS` and deployed as **v13**; sending is unblocked. (2) **notify-email v7 (live):** `contract_copy` is now write-once — skips if `jobs.contract_copy_sent_at` is already set and stamps with `.is(...,null)` guard (the Alberta 10-day clock can't be reset/spammed). (3) **Frontend bugs:** ClientDashboard rehire() pointed at nonexistent `/new-request` (404→home) → `/client-onboarding` (renders NewRequest for signed-in users); ContractorDashboard datetime prefill used `toISOString()` (UTC, 6–7h late in Calgary) → new `toLocalInput()` helper; milestone jobs no longer show the whole-job confirm-&-pay UI beside MilestonePanel (`!is_milestone` gate on pending_confirmation block). (4) **Legal accuracy:** PrivacyPolicy §6.2 table discloses Google sign-in + new Komoot GmbH (Photon, Germany) row, §7 mentions Germany transfers, §8 chat/media retention rewritten honestly (job media kept with transaction records up to 7 yrs; profile media best-effort 90 days); UserAgreement §6.3 now matches reality (charged at approval, held, released on confirmation — was "due upon completion"). (5) **UX batch:** MilestonePanel + ClientDashboard fee copy now read `platform_fee_rate()` (waiver-aware `feeText()`; no hardcoded "3%"); NewRequest scrolls to the first validation error (`nr-err-*` ids); ContractorDashboard per-job busy state (`busyJobId` replaces the global flag that froze every job's buttons) + STATUS_COLORS entries for `scheduled`/`pending_confirmation` (+ fallback); ContractorOnboarding step-7 now *renders* submitError (oversized-photo error was invisible) and both photo inputs reset `e.target.value` on reject so re-choosing the same file works. Validated: vite build ✓, typecheck 0 errors. Deploy script: `~/Desktop/Website/push-audit-fixes.sh`.

- **Vehicle + home blog expansion (2026-07-03).** Added 4 new posts to Blog.tsx (index) + BlogPost.tsx (article components) + sitemap.xml, filling the total gap in vehicle content (Freddy offers Vehicle Maintenance + Auto/Tires/PPF but had zero auto posts). New **"Vehicle"** tag (teal `#14b8a6`) added to TAG_COLORS in both files + a TAG_IMAGES hero. Posts: `basement-development-cost-calgary-2026` (Pricing; per-sqft ranges, lifestyle-vs-legal-suite, 2026 suite fee amnesty), `calgary-windshield-rock-chip-repair-cost-2026` (Vehicle; repair-vs-replace, no-deductible insurance, cold-crack urgency), `calgary-winter-tires-guide-2026` (Vehicle; 7°C rule, ~$1k set, 2-5% Alberta insurance discount), `winterize-your-vehicle-calgary` (Vehicle; battery/block-heater/fluids/kit checklist). The two winter posts cross-link and are kept distinct (tires-only vs everything-but-tires). Each has per-post meta in POST_DESCRIPTIONS + BarChart cost visuals; vehicle CTAs deep-link `/client-onboarding?service=Vehicle Maintenance`. Frontend-only — ships in the installer; no DB changes.

- **Audit pass: milestone job-completion on refund (2026-07-04).** Correctness audit of the money path. Fixed one latent edge case: `release-payment` (v13) and `refund-milestone` (v2) decided a milestone job was finished by checking that every stage was `released` — a **refunded** stage is terminal too, so a job with any refunded stage never auto-marked `completed`. Both now treat `released` OR `refunded` as terminal (`.not("status","in","(released,refunded)")`), and `refund-milestone` now runs the same completion check after a refund so refunding the last held stage closes the job. Live via MCP; installer ships both edge sources for version control. Rest of the audit came back clean: enabled RLS on `reminder_log` (the only ERROR advisor); all 5 pg_cron jobs firing; 24h of reconcile-payouts logs all 200; every RPC signature matches its frontend caller; typecheck 0 errors + vite build OK. Recommendations (not yet actioned): 7% commission is hardcoded in `propose_milestones` (client fee already uses `platform_fee_rate()` — consider a matching `platform_commission_rate()`); `notify-email` is `verify_jwt=false` so `contract_copy` is callable by anyone with a job_id (could reset the 10-day clock / spam) — consider gating or rate-limiting; `contractor-photos` + `portfolio-photos` buckets allow public listing.

- **"Estimate" rename + adjust-price-anytime-until-payout (2026-07-06).** (Follow-up after Fable-5 bug fixes.) TWO changes. (1) **Customer-facing "quote"/"quotes" -> "estimate"/"estimates"** across the whole site + legal docs (Home, ServicesIndex, ServiceLanding, BlogPost, GetQuote, AdminDashboard labels, RequestPhotoQuote, NewRequest, ClientOnboarding, MilestonePanel, UserAgreement §6.3/§6.10, ProfileCompletionModal, ContractorDashboard copy). Preserved: the `/get-a-quote` route path, DB/RPC/table identifiers (`submit_quote_lead`, `quote_leads`, `request_quote_revision`, `quote_assumptions`), component/prop names, and "estimated range/estimate" where it already read naturally. Fixed grammar ("a estimate"->"an estimate") and softened blog price-disclaimers to "ballpark ranges ... not firm estimates". Frontend-only.
  (2) **Contractors can adjust the price at ANY live stage, up until payout** — assigned / scheduled / in_progress / **and now pending_confirmation** (after they mark complete, while the client hasn't confirmed / money not yet released). Two-sided completion is preserved (contractor `mark_job_complete` -> client `confirm_job_completion` -> release; 3-day auto-confirm). **DB (live via MCP):** `jobs` +`price_change_pending jsonb` / `price_change_proposed_at` / `extra_charge_intent_ids text[]`; new `propose_price_change(...)` (contractor; blocks milestone/released/disputed; on **unpaid** job applies new price + back to `assigned` for schedule re-approval -> returns `'reapprove'`; on **held** job stores the pending jsonb `{amount,labour,parts,callout,subject,price_low,price_high,used_base_price,reason}` + notifies client -> returns `'pending_client_approval'`); `decline_price_change(...)` (client clears pending, notifies pro). Guards added: `confirm_job_completion` raises if a change is pending; `auto_confirm_stale_jobs` skips jobs with a pending change. **Edge (live via MCP; source committed here):** new **adjust-payment** (v1, jwt) — client approves a held-job change: increase -> Stripe Checkout for the delta (`kind:'price_topup'`, applied by webhook), decrease -> partial refund on the original PI + apply now, no-change -> apply now; preserves the original client-fee rate (0 if referral-waived), pays 93% of the FINAL amount. **release-payment v15** — job mode 409s if `price_change_pending` is set. **stripe-webhook v14** — `payment_intent.succeeded` `price_topup` branch applies `price_change_pending` (correct cols `labour_amount`/`parts_amount`/`callout_fee`/`subject_to_inspection`), appends the PI to `extra_charge_intent_ids` (idempotent), keeps `payment_status='held'`, notifies pro; failed-branch ignores `price_topup`/`recurring_prepay`/`milestone`. **Frontend (installer):** ContractorDashboard `requestRequote` now calls `propose_price_change` and handles both return modes; reusable `renderPriceChange(job)` shows the adjust-price block at scheduled/in_progress/pending_confirmation (hidden once `payment_status==='released'`) with a "waiting for the client to approve your new price" banner when pending. ClientDashboard gained an approve/decline price-change card (old->new price, delta, reason; Approve -> adjust-payment, redirects to Checkout if `mode==='topup'`; Decline -> `decline_price_change`) and disables the confirm-completion buttons while a change is pending. Validated: typecheck 0 errors + vite build OK.

- **Duplicate-account prevention by email + phone (2026-07-06).** One person can no longer hold two accounts. A single email OR a single phone number can back only one account, and the check is role-blind — a contractor can't re-register the same email/phone as a client (or vice-versa). **DB (live via MCP):** new `check_signup_availability(p_email,p_phone)` SECURITY DEFINER RPC (granted anon+authenticated) returns `{email_taken,phone_taken}` — email checked against `auth.users`+`profiles`, phone compared digits-only on the last 10 digits (so country-code prefixes like `1 403…` still match). Hard backstop: new `enforce_unique_signup_phone()` **BEFORE INSERT trigger on auth.users** raises `PHONE_TAKEN` if the signup metadata phone already belongs to a profile (email uniqueness is already enforced by auth.users' unique-email constraint; the AFTER-INSERT `handle_new_user` swallows errors so it can't be the guard). No hard UNIQUE index on phone — two existing seed accounts share a number, and a unique index would have failed to apply. **Frontend (installer):** ClientOnboarding + ContractorOnboarding call the RPC before `auth.signUp` and abort with a friendly per-field message ("An account with this email already exists…" / "…this phone number already exists…"); RPC failure never blocks signup (backstops still guard). Also added an **obfuscated-identities check** after `signUp` — with email-confirmation on, Supabase returns a fake success (`user.identities.length === 0`) for an already-registered email instead of an error, which the old `already registered` string-match never caught; now treated as a duplicate. Phone remains optional; the guard only fires when ≥10 digits are entered. Frontend-only in the installer; DB is live.

- **First-visit coach-mark tips (2026-07-06).** Two small pointer boxes greet **logged-out, first-time** visitors: one near the top-right **Settings gear** ("Customize" — theme/text size) and one near the bottom-right **chat bubble** ("Got a question?"). New `src/components/IntroTips.tsx` (mounted in App.tsx after ChatWidget): fixed boxes ~230px wide (`min(230px,76vw)`, ~10% of screen), NOT centered — anchored beside their target with a small triangle pointer; brand navy card + orange border + entrance animation. Shows only when `supabase.auth.getSession()` has **no session** and `localStorage['ff_seen_intro_tips']` is unset (set on first show → once ever); hidden on auth/onboarding/dashboard routes (`HIDE_ON` list). Each box has an × (dismiss that one) and a CTA that fires a `window` CustomEvent: **"Open settings"** → `ff:open-settings` (TopNav listens → opens SettingsModal) and **"Ask a question"** → `ff:open-chat` (ChatWidget listens → opens the chat). Frontend-only; ships in the installer.

- **Admin dashboard rework (2026-07-06).** Reworked AdminDashboard.tsx around the owner's actual workflow. (1) **New Accounts tab** (`admin_list_accounts()` SECURITY DEFINER RPC, admin-gated, granted authenticated) lists **every auth.users row** (not just profiles — so the 2 orphaned auth accounts show and can be removed) joined to profiles + contractors: name, role chip, email, phone (profile or signup metadata), join/last-sign-in dates, request/job counts, orphaned flag, and full contractor vetting (company, specialties, area, licence/insurance/WCB, references, status). Client-side **search** (name/email/phone) + **role filter** chips (all/client/contractor/admin/orphaned, each with a live count). Contractor **Approve/Deactivate** (via `admin_set_contractor_status`) and the re-signup poor-rating flag (from `admin_resignup_matches`) moved here. Each account has a **Delete account** button → new **admin-delete-account** edge fn (v1, verify_jwt=true, admin-gated via anon-getUser→profiles.role check) which **full-wipes**: deletes disputes (NO ACTION FK would block), jobs they're a party to (cascades milestones/messages/reviews), their client_requests (cascades bids/hidden_jobs/prepayments), recurring_prepayments, messages, the profile (cascades contractors/notifications/portfolio_items/reviews), storage under `${uid}/` across 6 buckets, then `auth.admin.deleteUser`. Blocks self-delete. (2) **Removed the standalone Contractors tab** (folded into Accounts) and the **admin assign-contractor dropdown + accept-bid buttons** — the client picks bids; bids now render read-only on the Requests tab with a "The client picks a bid" note. (3) **Jobs tab** gained a **Delete job** button → new `admin_delete_job(p_job_id)` SECURITY DEFINER RPC (cascades disputes/milestones/messages/reviews). (4) **Tabs reordered/decluttered** to `health, requests, jobs, accounts, disputes, prepaid, leads` (was `health, requests, contractors, jobs, disputes, prepaid, leads`); `counts`/pager reduced to requests+jobs (accounts load fully — small marketplace). DB RPCs + edge fn live via MCP; the installer (`apply-admin-rework.sh`) ships AdminDashboard.tsx + admin-delete-account source. Also cleared the lone stale pending request (Siding & Roofing) so the dashboard starts clean.

- **Dashboard + onboarding UX-friction batch (2026-07-07).** Post-celebration audit of the two dashboards + request/onboarding flows; fixed the real gaps (rest was already hardened). (1) **ContractorDashboard `load()` now has try/catch/finally + `loadError` + a Retry screen** mirroring ClientDashboard — a failed read used to hang the spinner forever or show a false "No jobs yet" (income-scary). (2) **`hideJob` now confirms** (`askConfirm`) — hiding an available job is permanent (writes `hidden_jobs`), so a phone mis-tap no longer silently buries a real lead. (3) **Native `alert()` replaced with an in-app toast on BOTH dashboards** — added a `notify(text, kind)` helper + fixed bottom-center toast (`toast` state, `useRef` auto-dismiss timer; green for ok / red for err, tap to dismiss); every `alert(` call site (~22 contractor, ~20 client) now routes through it. Success confirmations (job-confirmed, "on my way", "accept new time", reschedule-sent) surface as green toasts. (4) **`onMyWay` + `acceptReschedule` now give visible success feedback** (green toast) instead of silently updating state. (5) **ClientDashboard reschedule dropped `window.prompt`** for a styled inline textarea form (`showResched`/`reschedNote` → `submitReschedule`); the emoji was removed from the message content. (6) **ClientOnboarding scrolls to the first errored field** — added `co-err-<field>` ids to the 8 error `<p>`s + a `scrollIntoView` in `validate()` (reuses NewRequest's pattern) so a validation error on a long step is no longer stranded off-screen. (7) **Smaller fixes:** referral-copy failure now surfaces the code via toast (was swallowed); favourite-toggle failure now toasts (was silent); GetQuote name/email/phone inputs gained `autoComplete`/`type` for mobile autofill; contractor empty-state CTA relabelled "Update your specialties" (was "Check your availability & service area") to match the "add specialties" copy above it. Frontend-only; no DB/edge changes. Installer `apply-tighten-signup.sh` now also carries ClientDashboard.tsx + ClientOnboarding.tsx + GetQuote.tsx. Validated: typecheck 0 errors + vite build OK.
- **Contractor signup tightened + finish-signup nudge (2026-07-06).** Fable-5's "make signup easier" pass had let contractors create an account with almost nothing (a step-2 "In a hurry? Sign up now, finish later" fast-track that only needed name + specialties), and Google one-tap creates an `auth.users` row on click — so abandoned signups left **orphaned auth accounts with no profile** (e.g. jimmygeovanny.rivera@gmail.com). Two fixes. (1) **Full onboarding now required before the account exists.** Removed the step-2 fast-track card (+ its `agreeTermsQuick` checkbox and early `handleSubmit` button) from `ContractorOnboarding.tsx` — the ONLY submit path is now the final-step "Complete Registration". `validate()` gained **step-8 document enforcement**: government photo ID always required; insurance + WCB required when the chosen work type needs insurance; trade certification required when the work type needs a licence (`errors.docs` renders on step 8). Step-8 intro copy no longer says "you don't need them to finish signing up"; required-doc labels read "Required" (was "Needed before taking jobs"). Phone stays **optional** per owner. Steps 1-5 were already gated by `validate()`. (2) **Admin finish-signup nudge.** New **finish-signup-nudge** edge fn (v1, verify_jwt=false, **admin-gated** via anon-getUser→profiles.role): pages `auth.users` via the admin API, diffs against `profiles` to find orphans (no profile row), and emails each a friendly role-aware "finish setting up your account" message via Resend (CASL footer: mailing address + reply-to-unsubscribe). `{dryRun:true}` previews; `{confirm:"SEND"}` sends. AdminDashboard Accounts tab gained an orange info bar (shown when any orphan exists) with an **"Email a finish-signup reminder"** button (`nudgeIncomplete` → invokes the fn with the admin's JWT; `busyNudge` guard). Reusable for future OAuth orphans since one-tap can still create bare auth rows. **DB unchanged; edge fn live via MCP; installer (`apply-tighten-signup.sh`) ships ContractorOnboarding.tsx + AdminDashboard.tsx + finish-signup-nudge source + this file.** Validated: typecheck 0 errors + vite build OK. **Follow-up UX batch (2026-07-07):** after a contractor's insurance upload failed mid-signup, audited every uploader and shipped four fixes to `ContractorOnboarding.tsx` + `ContractorProfileCompletion.tsx`. (1) **Insurance-certificate upload on the Credentials step (step 6)** — inside the `{form.hasInsurance && (...)}` block, sets shared `docFiles.insurance` (auto-uploaded at submit by the existing `docKeys` loop, pre-fills step 8). (2) **HEIC-friendly uploads** — root cause of the failure: iPhone photos are HEIC by default and every *document* uploader used `accept=".pdf,.jpg,.jpeg,.png,.webp"`, which filters HEIC (only JobChat accepted it). Step 6, step 8, and the dashboard `ContractorProfileCompletion` card now all use `accept="image/*,application/pdf"` (lets the OS transcode HEIC→JPEG); labels updated to invite "take a photo". (3) **Client-side file-type validation** — new shared `docFileError(f)` in onboarding (+ inline check in `ProfileCompletion.pickDoc`) rejects non-PDF/non-image files with a friendly message (was size-only), resets the input so re-choosing the same file works, and allows pdf/jpg/png/webp/gif/heic/heif. (4) **Saved-draft recovery** — onboarding now persists text fields + selections + current step to `localStorage['ff_contractor_draft']` (password NEVER stored, files can't be) via a hydrate-gated save effect; a load effect restores on return and shows an orange "We saved your progress" banner with a **Start over** link (clears draft + reloads). Draft cleared on the verify-email branch, on success, and it never overwrites with empty (guarded by a `hydrated` flag). (5) **Upload busy state** — submit button now reads "Uploading your details…" while `loading`; the dashboard save button already showed "Saving…". Installer `apply-tighten-signup.sh` now also carries `ContractorProfileCompletion.tsx`. Frontend-only; no DB/edge changes. Validated: typecheck 0 errors + vite build OK. **Profile-complete celebration (2026-07-07):** when a contractor fills in the last missing required piece of their profile from the dashboard **Profile** tab, a confetti + "Congratulations! You completed your profile" moment fires (subtext, softened 2026-07-07 to "You're all set. We'll review your profile so you can start bidding for jobs"). New self-contained `src/components/ProfileCompleteCelebration.tsx` (canvas confetti, no dep; brand navy card + orange accents; "Browse available jobs" → jumps to the Available tab / "Maybe later" dismiss; confetti auto-stops after 6.5s). Trigger lives in `ContractorDashboard.tsx`: extracted a module-level `contractorMissing(c)` helper (shared by the "finish setting up" nudge and the trigger) and wrapped `ContractorProfileCompletion`'s `onSaved` to detect the incomplete→complete transition (fires `setShowProfileCongrats(true)` only when the save clears the last gap). Note: bidding still requires admin approval — copy says "can now start bidding" per owner request. Installer now also carries ContractorDashboard.tsx + ProfileCompleteCelebration.tsx. Frontend-only. Validated: typecheck 0 errors + vite build OK.

- **Admin alerts + admin document/media viewing (2026-07-07).** Two owner requests (chat photo/video was request #3 — already built/live in `JobChat.tsx`, no work needed). (1) **Email alerts to the admin** (`hello@freddyfixit.ca`) on new activity. New **admin-alert** edge fn (v1, verify_jwt=false — called by the DB, not end users; reads via service role, emails a fixed internal address) handles two events: `new_contractor` (name/company/email/phone/work-type/specialties/area) and `new_job` (service/location/timing/recurring/client name+email+phone + a 600-char description snippet), branded navy/orange Resend email from `noreply@freddyfixit.ca` with a CTA to the admin dashboard. Fired fire-and-forget from Postgres AFTER INSERT triggers via `net.http_post` (anon bearer, modeled on `kick_reconcile_payouts`): `admin_alert_new_contractor` on `public.contractors`, and `admin_alert_new_job` on `public.client_requests` (the job trigger **skips auto-generated recurring occurrences** — `recurring_source_date`/`recurring_parent_id` set — to avoid noise). Shared `enqueue_admin_alert(event,id)` SECURITY DEFINER helper wraps the http_post in an exception guard so a mail hiccup never blocks the insert. Verified end-to-end (edge returned 200 `{ok:true}`). (2) **Admin can view ALL contractor documents + photos** (owner chose "Everything"). `admin_list_accounts()` now also returns `doc_urls` (jsonb: gov_id/insurance/wcb/certification → storage paths in the private `contractor-docs` bucket), `photo_url` (public), and a `portfolio` array (title + `photo_path` in public `portfolio-photos`). New storage RLS policy **`contractor_docs_admin_select`** lets an admin `SELECT` the private `contractor-docs` bucket (owner-only before). AdminDashboard Accounts tab (inside the contractor card) now renders a **Documents** row — one signed-URL button per uploaded doc (`openDoc` → `createSignedUrl(...,3600)` → opens in a new tab; `DOC_LABELS` maps keys to friendly names, icon `download` since there's no `file` glyph in `Ic`) — and a **Photos** row of 60px thumbnails (profile photo + portfolio, click to open full via public URLs; `pubUrl` helper). Graceful "No documents or photos uploaded yet." when empty. Client job-request photos were already viewable in the Requests tab (RequestPhotoQuote). DB migrations + edge fn live via MCP; installer (`apply-tighten-signup.sh`, superset) ships the updated AdminDashboard.tsx + admin-alert source. Validated: typecheck 0 errors + vite build OK.

- **Guided "Freddy walks you through it" onboarding (2026-07-08).** Owner request: something that talks contractors and clients through sign-up / posting a job, makes it easier, but still collects every field. Chosen approach: a **scripted** (no AI, no cost, instant) conversational-coaching layer on top of the existing, proven multi-step forms — so no parallel submit/upload logic and zero risk of dropping data; the existing AI ChatWidget stays on the page for free-form questions. New self-contained `src/components/GuideBubble.tsx` — a friendly "Freddy" avatar + speech bubble that reframes each step in plain language, one thing at a time, with a **"Why we ask:"** reassurance line + optional tip, and a subtle "Step X of Y" pill (hidden when `total===1`). Re-animates on each step change. Wired into all three flows: `ContractorOnboarding.tsx` (`CONTRACTOR_GUIDE`, 8 entries — basics/specialties/area/availability/trade/credentials/photo/documents), `ClientOnboarding.tsx` (`CLIENT_GUIDE`, 3 entries — need/details/account), and `src/components/NewRequest.tsx` (single bubble for returning-client job posting). Rendered right after each page's header/progress bar. Frontend-only; no DB/edge changes. Installer `apply-tighten-signup.sh` (superset) now also carries GuideBubble.tsx + the two onboarding pages + NewRequest.tsx. Validated: typecheck 0 errors + vite build OK.

- **Supabase-style left-sidebar dashboards (2026-07-08).** Owner request: "change the dashboard UX for both dashboards — a tab on the left with all the options, use Supabase as inspiration." Reworked ClientDashboard + ContractorDashboard from a horizontal tab bar to a persistent left nav. New reusable `src/components/DashboardSidebar.tsx` — exports `type SidebarItem = { key; label; icon: IconName; badge? }` and a default `DashboardSidebar({ items, active, onSelect, title? })`. Desktop: sticky 232px column (`top:3.75rem`, `height:calc(100vh - 3.75rem)`, right border, faint fg tint), active row gets orange tint + `inset 3px 0 0 #ea6b14` accent + orange text/icon. **Mobile (<780px, per owner "collapse to icons, expand on tap"):** collapses to a 56px icon rail with a ☰ hamburger (three inline `<span>` bars — there is NO `menu` glyph in `Ic`) that toggles a fixed 216px labelled drawer over a scrim (z 900/901), closing on selection. `SettingsModal.tsx` refactored to expose `export function SettingsPanel({ role })` (the modal body as a fragment) so the client can render Settings as a dashboard tab; the default `SettingsModal` (TopNav) now just wraps `<SettingsPanel/>` in the overlay. **ContractorDashboard** nav = My Jobs / Available Jobs / Earnings / Reviews / Profile (`CONTRACTOR_NAV`); the always-on `<ProfileBar role="contractor"/>` moved into the Profile tab (above the ContractorProfileCompletion nudge). **ClientDashboard** nav = My Requests / My Pros / Recurring Plans / History / Profile / Settings (`CLIENT_NAV`); the request-history IIFE split out of the Requests block into its own `{activeTab === "history" && (() => {…})()}`; referral "invite a friend" card lives under Requests; My Pros + Recurring each got an empty-state; `<ProfileBar role="client"/>` -> Profile tab; the standalone `<DeleteAccount/>` was removed (it's inside SettingsPanel now, so the `DeleteAccount` import was dropped from ClientDashboard). Both content areas are wrapped in a `display:flex, alignItems:flex-start` shell with the sidebar + a `flex:1, minWidth:0` column. AdminDashboard is unchanged (its own tab system). Frontend-only; no DB/edge changes. Installer `apply-tighten-signup.sh` (now a 15-file superset) also carries DashboardSidebar.tsx + SettingsModal.tsx. Validated: typecheck 0 errors + vite build OK.

- **Sidebar footer actions + collapsible + no top-right Menu on dashboards (2026-07-08).** Follow-up to the sidebar rework. Owner request: "remove the menu button from the top right in the dashboards only and integrate these items into the sidebar; make the sidebar collapsible." (1) **TopNav** is now route-aware (`loc` from `useLocation`): on `/client-dashboard` + `/contractor-dashboard` the logged-in account **Menu** button is hidden (`authed && onSidebarDash ? null : …`); its items moved into the sidebar. `SettingsModal` stays mounted so the sidebar's Settings action can still open it via the existing `ff:open-settings` event. AdminDashboard keeps its Menu (no sidebar there). (2) **DashboardSidebar** gained a **footer** region: new `type SidebarAction = { key; label; icon: IconName; onClick; danger? }` + two new props — `bell?: React.ReactNode` (rendered with a "Notifications" label when expanded) and `actions?: SidebarAction[]`. Footer is bottom-anchored (`marginTop:auto`) with a top divider. (3) **Collapsible desktop rail:** new `collapsed` state persisted in `localStorage['ff_sidebar_collapsed']`; a bottom toggle («/») shrinks the column 232px↔64px (icon-only, `transition:width`). Mobile (<780px) behavior unchanged (icon rail + ☰ drawer; the collapse toggle is desktop-only). Extracted a shared `rowBase(labels)` style used by tab items, action buttons, and the toggle. (4) **Wiring:** ContractorDashboard passes `<NotificationBell userId={profile.id} dashboardPath="/contractor-dashboard"/>` + actions Contact us (`mailto:hello@freddyfixit.ca`) / Settings (`ff:open-settings`) / Log out (`handleSignOut`, danger, `door` icon). ClientDashboard passes the bell + Contact us / Log out (Settings stays a tab per the prior rework). Frontend-only; no DB/edge changes. Installer `apply-tighten-signup.sh` now also carries TopNav.tsx (16-file superset). Validated: typecheck 0 errors + vite build OK.

- **Welcome heading + incomplete-profile banner on dashboards (2026-07-08).** Owner request: "if there is incomplete information on any profile flag, put that at the top same as the payouts; add a button that takes them to the section they need to complete. also add 'welcome <username>' to the top of the dashboard." Both dashboards now open with a **"Welcome, {first_name}"** Bebas Neue heading (contractor shows account status underneath — pending/active/inactive; client shows a friendly subline). Below the header, a **persistent incomplete-profile banner** styled identically to the contractor payout warning (`background:rgba(234,107,20,.1)`, `border:1px solid rgba(234,107,20,.45)`, alert-triangle icon) appears when required profile info is missing, listing the gaps + a **"Complete profile"** button that jumps to the Profile tab (`setActiveTab("profile")` + `scrollTo top`). ContractorDashboard reuses the module-level `contractorMissing(c)` helper (service area / trade / licence-or-insurance / documents). ClientDashboard derives its own inline check (missing name → "your name"; missing phone → "phone number"). Frontend-only; no DB/edge changes. Installer `apply-tighten-signup.sh` unchanged file list (ContractorDashboard.tsx + ClientDashboard.tsx already carried). Validated: typecheck 0 errors + vite build OK.

- **Dashboard/home UX polish batch — 11 items (2026-07-08).** Owner approved all 11 suggestions from a UI-design review (two YouTube design videos + mobbin.com patterns); item 9 explicitly WITHOUT photos ("i dont have any faces") → initials avatars. (1) **"Needs your attention" action card** at the top of both dashboards (orange-bordered, max 3-4 rows, each row = icon + label + CTA button): contractor surfaces open unanswered claims (danger red "Respond now"), client-rescheduled times to review, assigned-but-unproposed jobs, visits within 24h, and reserved preferred-pro jobs; client surfaces pending price changes, jobs awaiting confirm, schedule proposals, and new bids. Built purely from data each dashboard already loads. (2) **Auto insight line** under the contractor 8-week earnings chart (orange, star icon): compares this week vs last vs best ("Best week in 2 months — $X, up Y%…" / "…one more job closes the gap"). (3) **Empty states that do work:** contractor jobs tab now shows a 3-step "Let's land your first job" checklist (complete profile / set up payouts / bid on N open jobs — steps go green with a check when done, each links to the right tab); client requests tab shows a "What needs fixing?" pitch + "Post your first request →" + popular-services line. (4) **Reliability streak** — client-side metric (consecutive most-recent completed+confirmed jobs with no claim, breaks on cancellation/dispute), shown as a 5th 🔥 stat card on contractor earnings with a tooltip. (5) **Dark-mode depth:** card surfaces lightened (`rgba(var(--ff-fg),.04)`→`.055`) with dimmer borders (`.08`→`.05`), orange chip saturation eased (bg `.1`→`.08`, border `.25`→`.18`) on both dashboards. (6) **Semantic color discipline:** red reserved for destructive/blocked (bid-cap "3/3 bids" now amber `#f59e0b`, was red; audited the rest — already clean). (7) **Typography:** dashboard headings capped (~1.7rem welcome, 1.5rem empty-state h2s), `lineHeight:1.1` + slight letterSpacing on big Bebas text, this-week hero clamp tightened. (8) **Button states:** scoped `.ffdash` class + `<style>` on both dashboard wraps gives every button hover brightness / pressed translateY / disabled opacity (inline styles can't do `:hover`); referral copy button flips to green "Copied ✓". (9) **Homepage vetted-pro cards** — new `get_top_pros(p_limit)` SECURITY DEFINER RPC (live via MCP; contractors join profiles, `status='active'`, rating desc nulls last, cap 12, anon+authenticated; returns contractor_id/company_name/first_name/rating/total_jobs/specialties — no contact info). Home.tsx fetches it alongside `get_homepage_reviews` (Promise.all) and renders initials-avatar cards (52px orange round, Bebas) with company/first name, ⭐ rating or "Vetted & approved", job count, first 3 specialties, in the Built-On-Trust section; self-hides when empty. (10) **New-vs-returning split** on the home client CTA: "Used Freddy before? Log in to rebook your pro →" link under the hero cards. (11) **Reassurance microcopy** (`.74rem` muted) under sensitive fields: GetQuote phone, ClientOnboarding phone + address, NewRequest new-address, ContractorOnboarding phone — each states exactly what the field is used for (address copy carefully avoids claiming contractors can't see location, since `list_open_jobs` shows it). DB: only the new RPC (live via MCP). Installer `apply-ux-polish.sh` ships ContractorDashboard, ClientDashboard, Home, GetQuote, ClientOnboarding, ContractorOnboarding, NewRequest + this file. Validated: typecheck 0 errors + vite build OK.

- **Admin sidebar parity + File-a-claim/Request-help footer actions + job IDs + collapsed tooltips (2026-07-08).** Four owner requests. (1) **AdminDashboard converted to the left `DashboardSidebar` layout** matching Client/Contractor: `ADMIN_NAV` (health/requests/jobs/accounts/disputes/prepaid/leads with icons check/clipboard-list/briefcase/user/alert-triangle/dollar/user-check), dynamic badges (open disputes / new leads / health alerts), a **"Welcome, {first_name} ADMIN"** Bebas heading, `NotificationBell` in the sidebar bell slot, and a **Log out** footer action; the old horizontal `s.tabs` bar was removed and the shell wrapped in the same `display:flex, alignItems:flex-start` + `flex:1, minWidth:0` column pattern with the `.ffdash` hover styles. `TopNav` now also hides the top-right Menu on `/admin-dashboard` (`onSidebarDash` includes admin). (2) **Collapsed-rail hover tooltips** — `DashboardSidebar` shows each icon's label as a `position:fixed` tooltip (`tip` state + `getBoundingClientRect`, rendered outside the overflow-clipped `<aside>`) when the desktop rail is collapsed or on the mobile icon rail. (3) **Contact-us footer action replaced per role** — clients get **"File a claim"** (`alert-triangle`) → new `src/components/FileClaimModal.tsx` (picks one of the client's jobs, each shown with its `jobCode`, then hands off to the existing `ReportProblem` → `open_dispute` flow); contractors get **"Request help"** (`message-square`) → new `src/components/RequestHelpModal.tsx` (in-app support form that emails admin **and** an "Ask Freddy" button that fires `ff:open-chat`). `ChatWidget` now hides only its floating bubble on dashboards (was fully unmounted) so `ff:open-chat` can still open the panel there. (4) **Human-readable job IDs** — new `src/lib/jobCode.ts` (`jobCode(id)` = `FFX-` + first 5 hex of the job UUID, uppercased; `codeMatches()` for tolerant lookup; **no DB column**). Shown on the client's Current Request card, the contractor's my-jobs cards, the admin Jobs cards, and the FileClaim picker so a client can quote it when filing a claim. **Edge (live via MCP; source committed in installer):** new **support-request** fn (v1, verify_jwt=true) — reads `{role,user_id,subject,job_code,message}`, looks up the sender via service role, emails `hello@freddyfixit.ca` via Resend with `reply_to` set to the sender, branded navy/orange HTML. Installer `apply-tighten-signup.sh` (superset) ships jobCode.ts + ChatWidget.tsx + RequestHelpModal.tsx + FileClaimModal.tsx + support-request source + AdminDashboard/ContractorDashboard/ClientDashboard/DashboardSidebar/TopNav. Frontend-only otherwise. Validated: typecheck 0 errors + vite build OK.

- **Contractor pipeline strip + calendar view (2026-07-10).** First two quick wins from the Jobber research (owner: "yea build the two"). (1) **Workflow pipeline strip** at the top of ContractorDashboard (shown on My Jobs / Available / Calendar tabs, above "Needs your attention"): six stage cards with live counts — New leads (availableJobs) → Needs your estimate (assigned w/o `schedule_proposed_at`) → Awaiting client (assigned + proposed) → Scheduled → In progress → Awaiting payment (pending_confirmation OR completed+`payment_status='held'`). Clicking "New leads" jumps to the Available tab; any other stage jumps to My Jobs and **toggles a stage filter** on the job list (new `jobFilter` state + module-level `STAGE_MATCH`/`STAGE_LABEL` shared by strip counters and filter so counts always equal what a click shows; filter bar shows "Showing N jobs — <stage>" + "Show all ×"; attention-card `goJob` and the calendar's onOpen clear the filter so they never land on a filtered-out job). (2) **Calendar tab** — new `{ key:"calendar", icon:"calendar" }` in CONTRACTOR_NAV (+ "calendar" in the activeTab union) rendering new self-contained `src/components/JobsCalendar.tsx` (props `{ jobs, statusColors, onOpen }`): Sunday-start month grid built from `myJobs` bucketed by LOCAL `scheduled_at` day (cancelled excluded), prev/Today/next nav, today = orange border, selected day = orange tint, ≤2 truncated job chips per cell (left border = STATUS_COLORS) + "+N more", a statuses-this-month legend, an "N active jobs have no booked time yet" nudge, and a selected-day detail card (time · service · client · location · $ · status + "Open job" → dashboard's goJob-style handler). No DB changes; frontend-only. Deploy script: `~/Desktop/Website/push-pipeline-calendar.sh` (repo edited in place). Validated: typecheck 0 errors + vite build OK.

- **Bid cap raised 3→7 + bids stay private (2026-07-10).** Owner: "allow contractors to bid on multiple jobs" (really: the per-request bid cap) + "don't let contractors see another person's bid." (1) **Per-job bid cap raised 3→7.** `place_bid` (SECURITY DEFINER) now uses `v_cap int := 7` — the check-then-insert cap (guarded by `pg_advisory_xact_lock(hashtext(request_id))`) rejects only when a request already holds 7 *distinct-contractor* bids (a contractor re-quoting their own bid via `on conflict (request_id, contractor_id)` is never blocked); the "job is now full" client notification moved from `v_cnt = 2` to `v_cnt = v_cap - 1` so it fires when the 7th bid lands. Contractors could always bid on unlimited *distinct* jobs — that was never capped. **notify-accepted** edge fn redeployed (v10, verify_jwt=false) with `const BID_CAP = 7` — the `three_bids` branch copy now reads "7 bids"; **event key kept as `'three_bids'`** for backwards-compat (place_bid still emits `event:'three_bids'`). Frontend `/3`→`/7` displays: ContractorDashboard available-jobs bid counter (`{bid_count}/7`, amber `#f59e0b` at cap + "This job already has 7 bids" gate), AdminDashboard read-only bids header (`Bids (n/7)`). SEO/marketing copy updated for accuracy: ServiceLanding.tsx (~24 "up to 3/three fixed-price estimates" + metaTitle "Up to 7 Estimates" + CTA), ServicesIndex.tsx (meta desc + hero + CTA), BlogPost.tsx (3 spots). (2) **Bids already private** — verified `bids` table RLS: contractors match only `bids contractor read own` (`contractor_id = auth.uid()`); clients read bids on their own request; admin reads all. `list_open_jobs` returns only the aggregate `bid_count` + the calling contractor's own `my_amount`, never another pro's number. No change needed. DB + edge live via MCP; installer `apply-tighten-signup.sh` (superset) ships the updated frontend. Validated: typecheck 0 errors + vite build OK.

- **Jobber batch #2: optional add-ons + on-site checklists + Get-set-up checklist + time tracking (2026-07-10).** Four more Jobber-inspired features (owner picked all four; AskUserQuestion decisions: hours billing = "Timer + adjust price" reusing the price-change flow AND the client must see tracked time + an explicit billed-by-the-hour notice; Calendly-in-welcome-email idea dropped). **DB (live via MCP, NOT in the push script):** `jobs.quote_items jsonb` ([{label,amount,optional:true,accepted:null|bool}]) + `jobs.checklist jsonb` ([{text,done}]) + `contractors.setup_skipped text[]` + new `job_time_logs` table (job_id/contractor_id/started_at/ended_at; RLS contractor-own / client-of-job / admin). RPCs: `propose_job_schedule` gained `p_quote_items jsonb default null` (old signature DROPped); `approve_job_schedule(p_job_id, p_selected_items int[] default null)` — **idempotent add-on math** (amount − previously-accepted + newly-selected, accepted flags set by index); `set_job_checklist(p_job_id,p_checklist)` (cap 40); `start_job_timer(p_job_id) returns uuid` / `stop_job_timer(p_job_id)`. **Frontend:** (1) *Optional add-ons* — contractor propose/re-propose form gained a dashed "Optional add-ons" editor (label+$ rows, cap 15, validation "give every add-on a price"); client approval card lists them as checkboxes with a live "Total if approved: $X" and passes `p_selected_items` (client-side optimistic update mirrors the server math; `selAddons` resets per job); accepted add-ons summarized on both dashboards' scheduled blocks; place_bid untouched; re-propose resets accepted→null. (2) *On-site checklists* — new `src/lib/checklistTemplates.ts` (GENERIC 8-item best-practices list + per-trade templates for all 22 service labels; `suggestedChecklist(serviceNeeded)`) + `src/components/JobChecklist.tsx` (contractor: add/toggle/remove + one-tap "Load suggested checklist" from the job's trade, optimistic save via `set_job_checklist` w/ rollback, progress bar; client: read-only progress, hidden when empty). (3) *Get-set-up checklist* — persistent card atop the contractor jobs tab (replaces the old "Let's land your first job" empty state): profile → payouts → availability → first bid, green checks, "X of 4 done" + progress bar, per-step "Do it" jump, **"I don't need this step"** skip persisted to `contractors.setup_skipped` (profile not skippable; skipped rows struck through); card hides when everything is done/skipped; a new `bids` head-count query in load() feeds first-bid detection. (4) *Time tracking + hourly billing* — new `src/components/JobTimer.tsx`: contractor start/stop timer per visit (multi-session, 1s live tick while running), total tracked time, and "Bill for tracked time" (nearest 15 min `Math.round(h*4)/4` × `contractors.hourly_rate`) which **pre-fills the existing propose_price_change/re-quote form** — no new payment path, client still approves; billing gated off milestone/released jobs; client mode shows tracked time read-only + the required notice ("this work can be billed by the hour … you'll see the new price and approve it before anything extra is charged"). JobTimer+JobChecklist mounted in the scheduled/in_progress and pending_confirmation blocks on BOTH dashboards. Deploy: `~/Desktop/Website/push-jobber-features.sh` (repo edited in place; also carries the pipeline-strip/calendar commit if not yet pushed). Validated: typecheck 0 errors + vite build OK.

## Gotchas
- esbuild build has **no typecheck**; rely on `vite build` for real errors. `any` is used liberally.
- Node/JSX authoring: avoid `${` inside template literals in generator scripts; for `$` amounts in JSX use `{"$" + x}`; embed CSS as `<style>{"...double-quoted css..."}</style>` (no backticks).
- Prior sessions shipped features that were never documented (DeleteAccount, NewRequest, the flags table). **Before building anything, check whether it already exists in the repo/DB.**

## Privacy / safety
- Repo is PUBLIC → never commit secrets (service-role key, etc.) or user PII (real contractor/client emails, phones, names). Reference seed rows by UUID; look up details in the DB when needed.
- Business contacts only: admin `hello@freddyfixit.ca`, from `noreply@freddyfixit.ca`.

## Open / queued
- **Stripe Connect payments — BUILT & LIVE (2026-06-26).** Separate charges+transfers via Stripe-hosted Checkout. Client pays Job price + **3% service fee**; funds held; on client confirmation (or 3-day auto-confirm) **93%** is released to the contractor and the platform keeps the **7%** commission. 6 edge functions deployed; `STRIPE_SECRET_KEY` switched to `sk_live`, single webhook destination set (`STRIPE_WEBHOOK_SECRET`). Test-mode Connect accounts were cleared so contractors re-onboard live payout accounts. Verified live with a real test charge.
- **Legal pages — BUILT & LIVE.** User Agreement (incl. Contractor Terms + §6.7 Fees), Privacy Policy (Alberta PIPA + PIPEDA; discloses Stripe/Supabase/Vercel/Resend; 7-yr payment retention; OIPC Alberta rights), and Homeowner Protection Promise. Routed (`/user-agreement`, `/privacy-policy`, `/homeowner-protection-promise`), linked in footer, and gated behind an acceptance checkbox in all signup flows (ClientOnboarding, ContractorOnboarding, NewRequest). Company entity: **Freddy FixIt Contractors Inc.**
- Set seed contractors' `company_name` + vetting answers in DB (owner to provide values).
- Keep this file + `src/CLAUDE.md` + `supabase/CLAUDE.md` current as features land.
